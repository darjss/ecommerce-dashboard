// import { validateSession } from "@/utils/helper";
// import { ProductWithDetailsAndImages } from "@/utils/types";
// import { ct } from "../db/queries";

// export const addProduct = async ( product: ProductWithDetailsAndImages) => {
//     const session = await validateSession();
//     if (!session) {
//         return { error: "Unauthorized" };
//     }

//     return await createProduct(product);
// }
